{
  "name": "gestorcoop-cron",
  "version": "1.0.0",
  "private": true,
  "description": "Worker de agendamento (Cloudflare Cron Triggers) do modulo de equipamentos.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --test-scheduled",
    "login": "wrangler login",
    "secret": "wrangler secret put CRON_SECRET"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
